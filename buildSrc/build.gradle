plugins {
    id 'java'
    id 'war'
}

description = "Defines all common build sources for the project modules."

repositories {
    mavenCentral()
}


dependencies {
    println("Adding Build-Common")
}